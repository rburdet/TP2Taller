#include "myMatrix.h"

void createMatrix(TMatrix* this,unsigned x,unsigned y){
	this->matrix = malloc(sizeof(char*)*x);
	unsigned i;
	for (i=0; i< x ; i++)
		this->matrix[i] = (char*)malloc(sizeof(char*)*y);
	this->dimX=x;
	this->dimY=y;
}
